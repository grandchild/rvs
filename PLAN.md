# Goals (Lukipuki)

## Short-term
- `types.rs` to define types used throughout the project.
- `traits.rs` to define interfaces of effects.
- 
### MVP
- create a minimal version of a simple scope visualization that implements the traits and types and renders on the CPU


## Next up
- implement a parallel version that renders on the GPU

## Long term



## Likely dependencies 
- argh for command line args
- winit for opening up windows (pro: cross platform, actively developed / contra: large)
- wgpu (gpu interfacing)
